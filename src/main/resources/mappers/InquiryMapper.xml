<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mtf.artixel.mapper.InquiryMapper">

    <insert id="insertInquiry" parameterType="com.mtf.artixel.vo.InquiryVO" useGeneratedKeys="true" keyProperty="inquiryId">
        INSERT INTO inquiry (category, client_name, country_code, contact, email, country,
                             artwork_title, artwork_size, artist_name, content,
                             file_url, file_path, file_origin_name, agree_privacy,
                             status, client_ip, created_at, updated_at)
        VALUES (#{category}, #{clientName}, #{countryCode}, #{contact}, #{email}, #{country},
                #{artworkTitle}, #{artworkSize}, #{artistName}, #{content},
                #{fileUrl}, #{filePath}, #{fileOriginName}, #{agreePrivacy},
                'PENDING', #{clientIp}, NOW(), NOW())
    </insert>

    <select id="selectRecentInquiryCountByIp" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM inquiry
        WHERE client_ip = #{clientIp}
          AND created_at >= DATE_SUB(NOW(), INTERVAL 1 MINUTE)
    </select>

    <select id="selectInquiryList" parameterType="com.mtf.artixel.vo.InquiryVO" resultType="com.mtf.artixel.vo.InquiryVO">
        SELECT
        inquiry_id AS inquiryId,
        category,
        client_name AS clientName,
        country_code AS countryCode,
        contact,
        email,
        country,
        artwork_title AS artworkTitle,
        artwork_size AS artworkSize,
        artist_name AS artistName,
        content,
        file_url AS fileUrl,
        file_path AS filePath,
        file_origin_name AS fileOriginName,
        agree_privacy AS agreePrivacy,
        status,
        client_ip AS clientIp,
        created_at AS createdAt,
        updated_at AS updatedAt
        FROM inquiry
        WHERE 1=1
        <if test="searchType != null and searchType != '' and keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'name'">
                    AND client_name LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'contact'">
                    AND contact LIKE CONCAT('%', #{keyword}, '%')
                </when>
            </choose>
        </if>
        <if test="startDate != null and startDate != ''">
            AND DATE(created_at) >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            <![CDATA[
            AND DATE(created_at) <= CONCAT(#{endDate}, ' 23:59:59')
            ]]>
        </if>
        ORDER BY inquiry_id DESC
        <if test="recordSize > 0">
            LIMIT #{limitStart}, #{recordSize}
        </if>
    </select>

    <select id="selectInquiryListCount" parameterType="com.mtf.artixel.vo.InquiryVO" resultType="int">
        SELECT COUNT(*)
        FROM inquiry
        WHERE 1=1
        <if test="searchType != null and searchType != '' and keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'name'">
                    AND client_name LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'contact'">
                    AND contact LIKE CONCAT('%', #{keyword}, '%')
                </when>
            </choose>
        </if>
        <if test="startDate != null and startDate != ''">
            AND DATE(created_at) >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            <![CDATA[
            AND DATE(created_at) <= CONCAT(#{endDate}, ' 23:59:59')
            ]]>
        </if>
    </select>

    <select id="selectInquiryDetail" parameterType="Long" resultType="com.mtf.artixel.vo.InquiryVO">
        SELECT
            inquiry_id AS inquiryId,
            category,
            client_name AS clientName,
            country_code AS countryCode,
            contact,
            email,
            country,
            artwork_title AS artworkTitle,
            artwork_size AS artworkSize,
            artist_name AS artistName,
            content,
            file_url AS fileUrl,
            file_path AS filePath,
            file_origin_name AS fileOriginName,
            agree_privacy AS agreePrivacy,
            status,
            client_ip AS clientIp,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM inquiry
        WHERE inquiry_id = #{inquiryId}
    </select>

    <delete id="deleteInquiry" parameterType="Long">
        DELETE FROM inquiry
        WHERE inquiry_id = #{inquiryId}
    </delete>

    <update id="updateInquiryStatus" parameterType="com.mtf.artixel.vo.InquiryVO">
        UPDATE inquiry
        SET status = #{status},
            updated_at = NOW()
        WHERE inquiry_id = #{inquiryId}
    </update>

</mapper>